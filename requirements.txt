# 核心依赖
pyinstrument>=4.0.0
flask>=2.0.0
requests>=2.25.0
pytest>=6.0.0
aiofiles==24.1.0
aiohttp==3.12.15

# 可选依赖
mattermostdriver>=7.0.0  # Mattermost通知支持

# FastAPI支持（可选）
# fastapi>=0.100.0
# uvicorn>=0.20.0
